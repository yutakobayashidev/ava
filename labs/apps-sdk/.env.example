# Development mode: Set this to your Vite dev server URL
VITE_DEV_SERVER_ORIGIN=http://localhost:5173

# Production mode: Custom path to manifest.json (optional, defaults to dist/manifest.json)
# VITE_MANIFEST_PATH=dist/manifest.json

# Server port (optional, defaults to 3000)
PORT=3000
